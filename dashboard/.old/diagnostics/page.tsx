// Frontend Web Application for RA Products
// Developed by R2 Labs

import DiagnosticsContainer from './diagnostics_container';

export default function DiagnosticsPage() {
    return <DiagnosticsContainer id={'diagnostics'} />;
}
