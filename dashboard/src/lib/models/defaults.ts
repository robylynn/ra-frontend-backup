import {
    ioConfigurationType,
    opcConfigurationType,
    uiConfigurationType,
} from '@/lib/models/autogenerated/configuration_models';
import { v4 as uuidv4 } from 'uuid'; // Import uuid for generating default component IDs

export const OLD_DEFAULT_UI_CONFIG: uiConfigurationType = {
    theme: 'system', // Default from schema
    tiles: [
        // At least one component is required by .min(1)
        {
            id: uuidv4(), // Generate a unique ID for the default component
            type: 'jogging',
            label: 'Welcome Widget',
            metadata: {
                message:
                    'This is a default widget. Please save your configuration to customize.',
                version: '1.0.0',
                pace: 5,
            },
        },
    ],
    plots: [],
    last_updated: new Date().toISOString(), // Current timestamp in ISO format
};

export const DEFAULT_UI_CONFIG: uiConfigurationType = {
    theme: 'dark', // Default from schema
    tiles: [
        // At least one component is required by .min(1)
        {
            id: uuidv4(), // Generate a unique ID for the default component
            type: 'camera',
            label: 'Camera Feed',
            tile_columns: 1,
            metadata: {
                camera_id: 'camera0',
                variant: 'full'
            },
        },
        {
            id: uuidv4(), // Generate a unique ID for the default component
            type: 'vision_statistics',
            label: 'Inference Statistics',
            tile_columns: 1,
            metadata: {
                source: 'camera0',
                refresh_interval: 5,
                time_span_hours: 1,
                defects_to_analyze: ['paint', 'corner'],
            },
        },
        {
            id: uuidv4(), // Generate a unique ID for the default component
            type: 'image_archive',
            label: 'Images',
            tile_columns: 1,
            metadata: {
                number_of_images: 6,
            },
        },
        {
            id: uuidv4(), // Generate a unique ID for the default component
            type: 'training',
            label: 'Training',
            tile_columns: 2,
            metadata: {
                source: 'model',
            },
        },
    ],
    plots: [],
    last_updated: new Date().toISOString(), // Current timestamp in ISO format
};

export const DEFAULT_IO_CONFIG: ioConfigurationType = {
    racks: [],
    last_updated: new Date().toISOString(),
};

export const DEFAULT_OPC_CONFIG: opcConfigurationType = {
    subscription_groups: [],
    last_updated: new Date().toISOString(),
};
