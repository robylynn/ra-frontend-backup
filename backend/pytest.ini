# backend/pytest.ini
[pytest]
# Configure pytest-asyncio to automatically manage event loops.
# This ensures a consistent event loop is available for all asynchronous fixtures and tests,
# applying the appropriate scope (session, function, etc.) automatically.
asyncio_mode = auto
