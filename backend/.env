# ./.env
# This file defines environment variables used by Docker Compose and the API.

# API Service Configuration
API_PORT=5000

# NEW: Secret key for JWT token encryption. GENERATE A STRONG RANDOM KEY!
SECRET_KEY="65bBOg6bPiYfVdsh/tSrkVHqYwz77ULAmK7oFG0G8F4=" # IMPORTANT: Replace with a strong, random key!

# NEW: JWT Token Configuration
ALGORITHM="HS256" # Algorithm for JWT signing
ACCESS_TOKEN_EXPIRE_MINUTES=30 # Access token expiration time in minutes

# TimescaleDB Configuration (Local - these are now primarily for Grafana/docker-compose internal use)
# Actual local DB URL will be read from the YAML specified by DB_CONFIG_FILE
# POSTGRES_DB=rac_db
POSTGRES_USER=user
POSTGRES_PASSWORD=password

# Grafana Configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin_password

# NEW: Database Configuration Management via YAML
# Path to the database configuration YAML file (relative to /app in container)
DB_CONFIG_FILE=config/database_configs.yml
# The specific deployment environment to use from the YAML (e.g., 'development', 'production')
DEPLOYMENT_ENV=development
